enum
====

Support for defining tagged unions and classical enums through the `enum`
sugar.

.. type:: Enum

   An opaque type.

   .. spice:: (__dispatch ...)
.. inline:: (Option ...)
.. sugar:: (dispatch ...)
.. sugar:: (enum ...)
